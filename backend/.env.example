# Master encryption key for manager passwords stored in SQLite.
# Must be exactly 64 hex characters (256-bit). Generate one:
#   PowerShell: -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
#   bash:       openssl rand -hex 32
REPORTTOOL_MASTER_KEY=
